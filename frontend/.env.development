# Development: use same-origin so Vite proxy sends requests to backend; cookies then work for PATCH/login.
# Leave empty to use relative /api/... (proxied to http://127.0.0.1:8000 in vite.config.ts).
VITE_API_BASE_URL=
